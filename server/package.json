{
  "name": "@cs2-modkit/gameface-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Direct-CDP MCP server that drives the Cities: Skylines II Gameface UI",
  "type": "module",
  "scripts": {
    "build": "bun build src/server.ts --target=node --format=esm --outfile dist/server.mjs",
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  }
}
