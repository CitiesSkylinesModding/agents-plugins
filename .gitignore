# NOTE: plugins/coherent-gameface/mcp/dist/server.mjs is intentionally committed so the plugin works
# with zero install. Do NOT add that dist/ here.

# One at the root and one in the mcp workspace.
node_modules
# Machine-specific settings
*.DotSettings.user

# In-flight specs and tickets: working material, not a record.
.scratch/

# Benchmark output: raw model answers and per-invocation summaries.
# A summary worth keeping is promoted into the repository by hand.
bench/results/
